bird(parrot).
bird(ostrich).

can_fly(parrot).

check_fly(Bird) :-
    can_fly(Bird),
    write(Bird), write(' can fly').

check_fly(Bird) :-
    \+ can_fly(Bird),
    write(Bird), write(' cannot fly').
